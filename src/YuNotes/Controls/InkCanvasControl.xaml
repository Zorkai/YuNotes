<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="YuNotes.Controls.InkCanvasControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:win2d="using:Microsoft.Graphics.Canvas.UI.Xaml">
    <Grid Background="{ThemeResource CanvasBackgroundBrush}">
        <ScrollViewer x:Name="Scroller"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollMode="Enabled"
                      VerticalScrollMode="Enabled"
                      ZoomMode="Enabled"
                      MinZoomFactor="0.25"
                      MaxZoomFactor="4.0"
                      HorizontalAnchorRatio="0.5"
                      VerticalAnchorRatio="0.5">
            <Grid x:Name="ContentRoot" Padding="40" HorizontalAlignment="Center">
                <StackPanel x:Name="PagesPanel" Spacing="24" HorizontalAlignment="Center" />
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
